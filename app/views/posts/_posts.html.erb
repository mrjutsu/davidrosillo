<%# @posts.each do |post| %>
  <tr>
    <td><%= post.name %></td>
    <td><%= post.body.html_safe %></td>
    <td><%= post.category.name %></td>
    <td><%= link_to 'Show', post %></td>
  </tr>
<%# end %>