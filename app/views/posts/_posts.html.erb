<article class="post">
	<hr>
	<h2>
		<%= link_to post.name, post_path(post.id) %>
	</h2>
	<i class="fa fa-tag post-tag">
		<span>
			<%= post.category.name %>
		</span>
	</i>
	<p class="post-description">
		<%= post.description.truncate(500).html_safe %>
	</p>
	<p class="post-rm">
		<strong><%= link_to 'Read More', post_path(post.id) %></strong>
	</p>
</article>